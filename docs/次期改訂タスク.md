# 次期改訂タスク（v0.7想定）v1.0 — 2026-07-13起草

skin-standard-01セッション中に確定・発見した、ベーステーマ＋プラグインの改訂タスク。
**次回セッションには catelabo-core（プラグイン）のzipも必要**（ページネーション対応のため）。

## 機能追加（設計判断は確定済み・手を動かすだけ）

1. **ページネーション（必須）**
   - `catelabo_kitten_list_query_args()` がcore側にあるため、posts_per_page確認から
   - テーマ側：`paged` の受け渡し＋`paginate_links` 出力＋`c-pagination` コンポーネント新設
   →解決済み(2026-07-14・v0.8.2)
2. **パンくずリスト**
   - 置き場所：子猫詳細・親猫詳細・下層ページのみ（TOP不要）。ヘッダー下に小さく
   - `BreadcrumbList` のJSON-LDを inc/seo.php の流儀で併設
   →解決済み(2026-07-14・v0.8.2)
3. **FAQパターン**
   - `<details>/<summary>` 方式で確定（アコーディオンのSEO不利は古い俗説。
     モバイルファーストインデックス以降、折りたたみ内も通常どおりインデックスされる。
     FAQリッチリザルトは2023年に一般サイトでは廃止済みのため開いておく理由もない）
   - Q/Aマーカーのデザイン＋固定ページ雛形.mdへのスニペット追記
   →解決済み(2026-07-14・v0.8.2)

## 修正・再検討

4. **親猫カードのhoverを全スキンで知覚できる変化に**
   - ベースは -4px に修正済みだが、影の変化がないため standard-01 以外では気づきにくい
   - 各スキンの世界観に合った hover 応答を検討（yuki=写真ズーム流用？ nostalgia=既存の彩度上げ？）
   →解決済み(2026-07-14・v0.9.0)
5. **ヘッダーナビとお問い合わせボタンのフォント不一致（default・yuki）**
   - standard-01 はナビを font-heading に統一済み。default と yuki は
     ナビ＝本文フォント／ボタン＝font-heading のちぐはぐが残る
   - どちらに寄せるか再検討して統一する（ベースで統一 or 各スキンで判断）
   →解決済み(2026-07-14・v0.9.1)
6. **defaultスキンの「線＋影」同時適用**（スキン推奨設定.md 宿題1参照）
  →解決済み(2026-07-14・v0.9.2)
7. **yukiのヒーロー額縁ズレ**（スキン推奨設定.md 宿題2参照）
  →解決済み(2026-07-14・v0.9.3)
8. **CTA折返し制御のベース昇格は完了済み**（2026-07-13）— 動作を全スキンで一巡確認
  →解決済み(2026-07-14・v0.8.0)

## 参考：今回セッションで確立した設計資産

- 色システムの思想：「選んだ色はCTA帯に原色で生きる。暗色はインクにも無変換。
  明色はインクを深めた派生に。面は染めない」（skin-standard-01設定ブロックのコメント参照）
- 一方通行の明度ガード：`min(l, x)` ＝暗色無変換・明色のみ沈む
- Sassバージョン非依存の書き方・ビルド落とし穴 → テーマ開発指針 §9-14〜16
